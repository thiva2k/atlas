# desktop/theme

Installs the Atlas dark KDE color scheme asset at
`$XDG_DATA_HOME/color-schemes/Atlas.colors`.

Atlas does not change the active KDE color scheme or overwrite user theme
preferences. `remove` deletes only the unchanged Atlas-owned asset and detaches
the marker.
