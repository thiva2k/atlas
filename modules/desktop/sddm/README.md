# desktop/sddm

Installs an Atlas SDDM theme and an Atlas-owned SDDM config drop-in selecting
that theme. Atlas refuses unmanaged files at its target paths and removes only
unchanged Atlas-owned files.
