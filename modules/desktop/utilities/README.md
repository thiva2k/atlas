# desktop/utilities

Installs missing engineering utility packages: `btop`, `bat`, `fd-find`,
`ripgrep`, `eza`, and `zoxide`.

Atlas never edits aliases, shell startup files, or user tool configuration.
